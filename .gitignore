# 依赖
node_modules/

# 管线产物（可复现：clips=录屏素材 cards=静态图层 render=成片；成片留存拷去稳定盘位）
clips/
cards/
render/

# 媒体二进制兜底
*.mp4
*.mov
*.mp3
*.wav
*.gif

# 本机会话配置不入库（.claude/skills/ 是入库源，须保留 tracked）
.claude/settings.local.json
.claude/settings.json

.DS_Store

# 公众号粘贴用 HTML（可由 tools/md2wx.py 从 md + img 重新生成，7MB base64 不入库）
copy/wechat/wechat-paste.html
