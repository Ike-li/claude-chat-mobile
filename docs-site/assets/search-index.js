window.__SEARCH_INDEX__=[{"url":"index.html","title":"封面与导读","part":"起点","lead":"一句话认识项目，看清这本手册怎么读。","text":"◆ 起点 封面与导读 Claude Chat Mobile 项目全景手册：认识项目、阅读路线与事实基线。 站点 SEO/内容面更新 2026-07-23 · 源码基线 dev@2de707b · 维护者 Ike-li Claude Chat Mobile 是一层默认上锁的转发层：把你本机已登录的 claude CLI，接到手机浏览器。同一会话、同一份 transcript、同样的权限与配置。 1 用户 / 实例 0 多租户设计 2000 事件环形缓冲 28 本手册页数 定位 终端等价入口 不是远程桌面，也不是 Claude 的重新实现。手机上打字，应与坐在终端前等价。 形态 自托管桥 Node ≥20 · Express · Socket.IO · Agent SDK 驱动本机 CLI。每实例单用户。 手册 按认知顺序 认识 → 演进 → 方法论 → 实现 → 数据 → 运维 → 规范。以 dev 代码为事实基线。 这本手册从哪来 源码分支为 dev （探针时 HEAD 2de707b ）。 docs/ 在 2026-07-19 后只保留 deployment.md 与生成的 repository-map.md ，设计长文已撤。本站按代码与 README / CLAUDE.md 重叙述，不照搬旧文档目录。 事实基线（非自动同步） 手册关键数字与契约以探针时的 dev 代码为准（HEAD 2de707b ）。dev 继续演进后，本站 不会 自动重生成；若行为与手册冲突，以当前代码与 scripts/agent-event-contract.js 为准，并应回写本站或记入漂移清单。 范围 本手册描述产品源码（dev），不是 gh-pages 宣传站本身。产物落在 gh-pages 的 docs-site/ ，便于与展示站同分支维护。 三条阅读路线 主线 完整通读 · 约 2–3 小时 侧栏从上到下，或一路点「下一页」。适合真要接手或 fork 的人。 速览 30 分钟 封面 → 总览 → 架构 → 职责边界 → 终端等价 → 安全模型 → 快速开始 → 漂移清单。见 阅读顺序指南 。 角色 按职责切入 运维先看第六部分；改同步引擎看「单驾驶员」与「事件信封」；改鉴权看第四部分鉴权页。 技术栈一瞥 层 选型 运行时 Node ≥20 · ESM · Express · Socket.IO 4 Agent @anthropic-ai/claude-agent-sdk 0.3.201 → 本机 claude CLI 鉴权 AUTH_TOKEN · 设备 TOFU · 可选 Cloudflare Access（jose JWT） 通知 web-push（VAPID）· 可选 ntfy 测试 node --test · Playwright 移动端 E2E（DOM 断言，非像素） 许可 AGPL-3.0-only + NOTICE 补充条款 从 阅读顺序指南 或 项目总览 继续。"},{"url":"pages/reading-guide.html","title":"阅读顺序指南","part":"起点","lead":"主线通读、30 分钟速览、按角色切入。","text":"读者打开手册后应立刻知道：怎么读、读哪条线、按什么顺序。主线兜底，速览与角色线是捷径。 主线通读 主线 = 七部分的天然顺序，与侧栏一致： 预计 2–3 小时。适合第一次完整接手。每页可独立读懂，但仍建议按序建立心智模型。 30 分钟速览 封面与总览。 封面 → 项目总览 ：一句话定位与场景。 架构与边界。 系统架构 → 职责边界 ：分层与「不做什么」。 方法论三页。 终端等价 → 安全模型 → 单驾驶员 。 能跑起来。 快速开始 ；若要公网再扫 生产部署 标题。 校准事实。 漂移清单 ：文档与代码不一致处以代码为准。 按角色切入 角色 先读 再补 自托管运维 快速开始、生产部署、运维排错、环境变量 安全模型、鉴权与设备 后端 / 同步 架构、AgentSession、事件信封、单驾驶员、数据全景 会话与历史、契约门禁 前端 / PWA 前端与 PWA、事件信封、API 事件参考 通知与状态栏 安全审查 安全模型、鉴权与设备、文件上传、职责边界 生产部署中的反代警告 贡献者 项目规范、契约门禁、术语表 演进主线、漂移清单 事实基线约定 以代码为准 手册基于 dev 源码交叉核对（探针 HEAD 2de707b ）， 不是 持续自动同步。注释里若仍出现 docs/design.md ，该文件已从仓库移除；权威安全叙述在 README「安全模型」节。矛盾点集中记在 漂移清单 。 搜索 任意页按 ⌘K / Ctrl+K 打开全文搜索。深浅色由顶栏切换，偏好写入 localStorage 。"},{"url":"pages/overview.html","title":"项目总览","part":"第一部分 · 认识项目","lead":"定位、适用场景、技术栈与仓库形态。","text":"项目给已经在终端使用 claude CLI 的人用：在手机上对同一 agent 打字，效果与坐在电脑前等价。 一句话定位 移动端聊天式 Web UI，通过 Claude Agent SDK 驱动 本机已登录的 claude CLI。不打包 Claude，不重实现 agent 循环；继承同一份 CLAUDE.md 、MCP、skills、hooks 与会话 transcript。 适用场景 任务在跑，人离开电脑。 审批与提问推到手机，点允许或拒绝。 同一会话，换设备继续。 手机与终端读同一份 CLI 会话记录；路上起头，回家 /resume 接上。 多仓库并行。 白名单内切换工作目录，标签页看多个会话进度。 手机输入更顺。 斜杠命令、相册附图、长按复制等按触控设计，而非缩小终端。 若只是偶尔远程看一眼屏幕，远程桌面通常够用。本项目面向「把手机当成终端随身入口」的高频使用。 技术栈 运行时 Node ≥20 · ESM · Express · Socket.IO 4 · compression · dotenv Agent @anthropic-ai/claude-agent-sdk 0.3.201 · 本机 claude 鉴权 / 推送 jose 6（Access JWT）· web-push（VAPID）· 可选 ntfy 前端 原生 ESM PWA；vendor 自托管（Tailwind / marked / highlight.js / DOMPurify） 测试 node --test 单测/集成 · Playwright 移动端 E2E · smoke 真 CLI（默认不跑） 许可 AGPL-3.0-only · NOTICE Section 7 补充条款 仓库形态 路径 角色 server.js 兼容启动器（npm / LaunchAgent / systemd） src/ 服务端实现，按域分包 public/ Express 直接托管的 PWA scripts/ 维护命令，非运行时 tests/ 唯一自动化测试根 docs/ 极简：deployment + repository-map deploy/ plist 模板 inventory 分类文件约 277 个（见生成的 docs/repository-map.md ）。后端源约 44 文件，前端源约 21，单测约 89，E2E 约 30。 分支纪律 dev ：日常开发。 master ：稳定 / 默认克隆目标；有分支保护；经 scripts/release.sh 从 dev ff 合并并发版。 gh-pages ：宣传站孤儿分支（本手册产物所在）。 promo ：宣传文案创作区，非产品源码。 附注 仓内 .worktrees/ 是其他分支的常驻检出位，不是 dev 源码。在 dev 上搜索/审查应排除该目录。 前置条件 Node ≥20。 node --version 检查。 本机可用的 claude CLI。 which claude 且能登录对话；项目不自带 CLI。 订阅或网关。 官方订阅沿用 CLI 登录态；第三方网关在 启动 server 的 shell 中 export ANTHROPIC_* （写入 .env 会被剥除）。 macOS 或 Linux。"},{"url":"pages/architecture.html","title":"系统架构","part":"第一部分 · 认识项目","lead":"从手机 UI 到本机 CLI 的分层与数据路径。","text":"系统是一条默认上锁的转发链：手机 UI 经 Socket.IO 到本机 server，再经 Agent SDK 驱动 claude CLI 读写你的工作区。 分层总览 flowchart LR subgraph Phone[\"手机\"] UI[\"public/ 单页\\n气泡·工具卡·审批\"] end subgraph Edge[\"公网可选\"] CF[\"Cloudflare Tunnel\\n+ Access\"] end subgraph Host[\"本机\"] S[\"server.js + src/server\\nExpress · Socket.IO · 鉴权\"] A[\"AgentSession\\nSDK query · 权限闸 · 信封缓冲\"] SDK[\"claude-agent-sdk\"] CLI[\"本机 claude CLI\"] FS[\"WORK_DIR 项目文件\"] J[\"CCM_DATA_DIR\\n元数据\"] end UI |\"agent:event / user:*\"| CF CF S UI |\"LAN #token\"| S S A A SDK SDK CLI CLI FS S --- J 后端域划分 目录 职责 src/server/ 组装根（sink）：HTTP 壳、Socket 契约、实例生命周期与业务接线 src/agent/ SDK 桥、审批台账、消息去重、模型缓存、CLI mirror src/auth/ CF Access JWT、设备 TOFU、鉴权限速 src/sessions/ 会话元数据、transcript 历史、工作区白名单、worktree 发现 src/files/ 上传、只读浏览/预览、路径闸 src/ops/ 审计、指标、推送、statusline、doctor src/shared/ 叶子：日志时间戳、脱敏、串行写盘 src/server 是唯一组装 sink：只有 server.js 与 src/server/* 可 import 它。域模块不得反向依赖 server。边界由 scripts/check-import-boundaries.js 在 npm run check 中硬闸。 消息流程（五步） 发送。 手机 user:message {text} → server 校验 → 路由到 agents.get(instanceId) （懒开 FRESH 或 resume）。 驱动。 文本进入 AgentSession 的 streaming input → SDK → CLI 在 WORK_DIR 执行。 映射。 SDK 消息经 map() ：流式文本 → text_delta ；工具 → tool_use / tool_result ；白名单外 → permission_request 。 信封。 每个事件套 {seq, epoch, sessionId, instanceId, cwd, ts, type, payload} → 入 2000 条环形缓冲 → io.emit （前端按 viewingInstanceId 分流；后台 tab 的高频 delta 不广播以省带宽）。 补发。 断线重连 sync:since {lastSeq} 从缓冲回放； epoch 变化表示服务端换了实例，客户端重置去重基线。 启动路径 留意 无 AUTH_TOKEN 时只绑定 127.0.0.1 ，手机与隧道均不可达。这是有意设计，不是故障。 前端结构 public/js/app.js ：大壳，socket 与 UI 编排。 public/js/logic.js ：纯决策（禁 DOM/socket），与 node:test 共用。 public/js/canonicalize.js ：前后端共享操作规范化（边界白名单）。 public/js/app/* ：会话、附件、审批、设置、通知等子控制器。 public/vendor/ ：第三方库本地自托管，不依赖 CDN。"},{"url":"pages/boundaries.html","title":"职责边界","part":"第一部分 · 认识项目","lead":"做什么、不做什么；与 CLI / 云 / SaaS 的分界。","text":"接手者最需要的认知往往是「这事归谁」。本节把职责边界写死，避免按 SaaS 或远程桌面去设计。 做什么 把本机 claude CLI 接到手机浏览器（聊天 UI、工具卡片、审批、提问）。 与终端共享同一会话 transcript（ ~/.claude/projects/… ）与配置来源（user / project / local settings）。 在 CLI 权限未命中时，把审批推到手机；命中则自动放行（与终端同源）。 提供多工作区白名单、多会话标签、附件上传、PWA 与尽力而为的推送。 提供 doctor / health / metrics 等运维面，便于自托管排查。 不做什么 不做 说明 多租户 / 账号体系 / Hosted SaaS 每实例单用户；任何通过鉴权的请求 = 坐在终端前的你 Claude 重实现 不自带模型服务；不维护平行 agent 协议栈 项目级 allowedTools 放行表 不注入 allowedTools / disallowedTools ；放行集 = 用户已有 settings 的并集 无 token 的公网开放 未设 AUTH_TOKEN 只绑 127.0.0.1 把 CLI transcript 迁入仓库 控制面在 CCM_DATA_DIR ；消息正文仍在 ~/.claude 跨实例联邦 / 远程管理 SaaS 运维面有意克制 与相邻系统的分界 flowchart TB CCM[\"Claude Chat Mobile\\n转发 + UI + 审批\"] CLI[\"claude CLI\\n执行 · 权限 · MCP · skills\"] CF[\"Cloudflare\\n隧道 + Access\"] Push[\"Web Push / ntfy\"] UserFS[\"用户工作区文件\"] CCM -->|\"SDK query\"| CLI CLI --> UserFS CF -->|\"HTTPS 到 :3000\"| CCM CCM --> Push Claude CLI 真实执行者；权限策略与登录态的权威在 CLI 侧 Cloudflare 可选公网入口与 2FA；项目不内置 cloudflared 生命周期管理 通知通道 尽力而为；失败不阻断主对话循环 安全相关边界（摘要） 单用户。 不要当多租户服务部署。 无 token 不出本机。 权限只继承 CLI。 公网暴露前审查 ~/.claude/settings.json 白名单。 设备 TOFU。 合法 token 不够；非本机设备须一次性批准。反代到 loopback 会跳过设备层——公网须 Access 或自觉强 token。 完整叙述见 安全模型 与 README 对应章节。"},{"url":"pages/evolution.html","title":"架构演进主线","part":"第二部分 · 演进历程","lead":"同步引擎、单驾驶员、门禁、文档极简等关键阶段。","text":"今天的架构来自一串可观察的决策，而不是一次性设计文档。以下主线由 git 历史与现行代码交叉归纳。 主线阶段 桥接层 Web ↔ Agent SDK ↔ CLI 以 streaming input 与事件映射为中心；server 作为契约层而非业务 agent。 同步 信封 · 缓冲 · catch-up seq / epoch 信封、2000 条环形缓冲、 sync:since 重连补发；断线可恢复。 驾驶权 单驾驶员 + 只读镜像 Web 与终端不可同时写同一会话；CLI 驾驶时 Web 只读追平 transcript，发送路径接管。 安全纵深 token · TOFU · Access 无 token 绑 127.0.0.1；设备指纹审批；可选 Cloudflare Access JWT fail-closed。 工程门禁 check 流水线 ESLint、import 边界、双向事件契约、inventory、文档一致性；AI 会话下防止结构回缠。 文档极简 宣传层撤出主分支 2026-07-19 起设计长文与演示场景下线；产品 docs 只留部署与 inventory；宣传物迁 gh-pages / promo。 近期可观察提交主题（dev） 主题 代表方向 同步 / mirror externalDirty、镜像锁自动释放、busy reseed、重连误判修复；2026-07-20：镜像误重锁、catchUpTickOnce 状态提交延后到视图守卫后 消息子系统 pendingTurns 双扣修复、send/cancelQueued 视图守卫、pendingSendBusy 跨会话隔离、message-dedup TOCTOU；control_request 超时保护扩到 setModel/setPermissionMode/stopTask/cancelAsyncMessage 状态可见性 CLI 式动态状态行、回合收尾行、服务状态面板判定化；限流重试中点停止不再卡「正在停止…」 后台 / 子代理 多子代理并行时防 idle 误杀与误解锁；bgTasks 刷新 lastActivity；生命周期文案理顺 worktree 会话触达接线；首页「最近活跃」纳入 worktree 会话（冷启动一键续） 排队与交互 排队可见、撤回重编辑、原生 confirm 改 sheet 运维 日志时间戳、copy-truncate 轮转、客户端错误上报 仓库边界 宣传层下线、demo 场景拆除、.worktrees 约定 手册探针基线：dev@2de707b（自 8084005 起含上述消息子系统审查修复与 worktree 首页）。 有意保留的「上帝模块」 src/server/app.js 仍是组装根本体：mirror / catchUp / openInstance 与契约路由共享同一组顶层可变状态（viewing*、mirror*、catchUp*）。域逻辑已下沉，但接线有意不拆成多个上下文穿针——注释与边界守卫共同说明这一点。 附注 代码注释中大量 docs/design.md / AD-x / NFR-x 溯源标记是历史决策锚点；文件本身已删除，标记有意保留，勿当作仍存在的文档路径。 分支与产物解耦 产品开发线：master / dev。 展示：gh-pages 孤儿分支（landing + 本手册）。 文案：promo 孤儿分支。 宣传物与产品源码解耦，避免主分支再次被素材与生成脚本淹没。"},{"url":"pages/terminal-parity.html","title":"终端等价性","part":"第三部分 · 方法论","lead":"CLI 有什么 Web 就有什么；权限与配置同源。","text":"Claude Chat Mobile 的产品目标是终端等价：CLI 有什么，Web 就有什么。实现上不重造 Agent 能力，而是用 Agent SDK 驱动本机已登录的 claude CLI，并与 CLI 共用配置、权限与 transcript。 等价原则 当你不知道怎么处理功能时，CLI 有什么 web 就有什么，请找一找 claude code cli 是怎么实现这个功能的。—— CLAUDE.md 本项目不是远程桌面，也不是另一套 Claude 客户端。手机上的一次发送、一次审批、一次切模型，应与坐在本机终端前操作同一会话等价。遇到「Web 还缺什么」时，默认动作是 找 CLI 现有实现与配置面 ，而不是在转发层另开一套语义。 01 同源驱动 Agent SDK 拉起本机 CLI 子进程，而不是另写一套工具循环。 02 同源配置 settingSources: ['user','project','local'] ，与 CLI 读同一组 settings。 03 同源会话 transcript 落在 ~/.claude/projects ，与 CLI /resume 同一事实源。 配置加载：settingSources 含义与 CLI 一致：用户级、项目级、本地覆盖依次参与合并。模型默认、权限白名单、env 块等由这些 settings 决定，转发层不维护平行配置权威。 建议 改权限或默认模型时，先改 ~/.claude/settings.json 或工作区 .claude/settings.json[.local] ，再在 Web 验证；不要在 CCM 里另造白名单文件。 权限：不注入 allowedTools SDK 选项 不注入 options.allowedTools 。放行白名单完全交给 settingSources 加载的 settings 里 permissions.allow 并集（与终端 claude 同源、由用户自管）。SDK 把 settings 的 allow 当作「自动放行、不触发 canUseTool」的第一层；未命中则进入统一闸门 canUseTool。 层 行为 settings permissions.allow 命中则自动放行，不弹手机审批 canUseTool 白名单外统一闸门；按当前权限档 allow / deny / 弹窗 转发层自建白名单 不维护；避免与 CLI 分叉 留意 公网暴露前务必审查全局 ~/.claude/settings.json 。终端里多年累积的 Bash(…) / Write 规则在手机侧也会自动放行，不会再弹审批。 权限档 代码白名单（ setPermissionMode / CCM_PERMISSION_MODES ）为 六档 ： 档 含义 default 默认审批；白名单外走 canUseTool 弹窗 plan 计划模式，与 CLI plan 语义一致 acceptEdits 接受编辑类操作的放宽档 bypassPermissions 实例记真实 bypass；给 SDK 映射为 default ，由 handleCanUseTool 直接 allow（避免 SDK 危险全局 flag） dontAsk Web 专属严格档：白名单外 deny、手机不弹窗；CLI transcript 不会出现 auto SDK 用模型分类器自动批准/拒绝权限请求 从 CLI transcript 恢复时，可读档为 default / plan / acceptEdits / bypassPermissions 等 CLI 可写出的档；dontAsk 属 Web 专属，不会从 CLI transcript 读回。 会话与 transcript CCM 的 sessions.json 只存元数据，不存消息正文。列表与历史冷读与 CLI /resume 同源扫描该目录；历史封顶 HISTORY_MAX_MESSAGES = 2000。 模型与 effort 模型可在发送路径切换。思考强度 effort 在 spawn 时注入 SDK options（与终端 /effort 同旋钮）； SDK 无运行时 effort 控制请求 。Web 侧 user:setEffort 的切档路径是 server dispose 当前实例 + 同 sessionId resume 新实例 （instanceId / epoch 会变），不是 in-place 改活进程。新会话（FRESH）的默认档来自 CLI settings 合并结果，而不是「继承本 cwd 上一个 Web 实例的末档」——贴近「新起一条 claude 是干净默认」。 驱动 Agent SDK → 本机 claude CLI 配置源 user / project / local 工具白名单 settings permissions.allow 并集，不注入 allowedTools transcript ~/.claude/projects 发送与接收路径 刻意不对齐之处 手机 UI 是触控优先的聊天壳，不是终端仿真器（斜杠命令、相册附图、长按复制等）。 Web resume 是冷读 transcript，不能 attach 正在运行的 CLI 活进程（见 单驾驶员 ）。 通知、PWA、设备 TOFU 是桥接层能力，CLI 本身没有对应物。 状态文案 / spinner / statusline 尽量对齐 CLI 语义，交互壳可以是手机 UI，但权限与会话语义不得分叉。"},{"url":"pages/security-model.html","title":"安全模型","part":"第三部分 · 方法论","lead":"单用户、token 不出本机、权限继承 CLI、设备 TOFU。","text":"安全模型围绕一句话展开：这是 每实例单用户 的自托管桥，不是多租户 SaaS。任何通过鉴权的请求，都与机主本人坐在本机终端前拥有同等权限。权威叙述在 README「安全模型」节。 危险 这是一条可远程触达、直通本机 shell 的代码执行通道。暴露到公网前必须完成 token、设备门、CLI 权限与（建议）Cloudflare Access 的审查。 页面更新 2026-07-23 · 维护叙述对齐 README「安全模型」· 维护者 Ike-li 四条铁律 每实例单用户。 无多用户、账号或登录系统。通过鉴权 = 与坐在终端前同等权限。不要当多租户服务。 没有 token 就不出本机。 未设置 AUTH_TOKEN 时服务只绑定 127.0.0.1。不存在「留空 = 对全世界开放」。 权限只继承 CLI。 项目不维护独立放行/禁用清单。放行 = 用户已有 settings 的 permissions.allow 并集。 设备信赖（TOFU）。 既非本机、也未经 Cloudflare Access 验证的连接，须在电脑上一次性授权该设备。合法 token 本身不够。 绑定地址 条件 listen host 含义 AUTH_TOKEN 非空 0.0.0.0 局域网与隧道可达 未设或空串 127.0.0.1 仅本机；手机/隧道进不来 默认端口 PORT = 3000。 权限继承 CLI 工具放行与拒绝继承本机 CLI 的 settings 与权限档，不另建「Web 专用超级白名单」。公网暴露前应审查： 重点看 permissions.allow 是否含过宽规则（如无限制 Bash）。运行中 UI 体检（doctor-runtime）会对危险白名单做脱敏摘要；启动前 CLI doctor（D1–D11）不替代该项。 鉴权分层 flowchart TB A[\"连接进入\"] --> B{\"Host 是否公网 Access\"} B -->|是| C[\"校验 CF Access JWT\"] B -->|否| D[\"校验 AUTH_TOKEN\"] C --> E[\"限速状态机 onAuthResult\"] D --> E E -->|通过| F{\"本机或已批设备?\"} F -->|否| G[\"TOFU pending\"] F -->|是| H[\"进入业务事件\"] HTTP（/health、/metrics、/push/*）与 Socket 握手共用 token 匹配与限速 Map。 Cloudflare Access：仅当 CF_ACCESS_HOSTNAME / TEAM / AUD 三项齐全 才启用；缺一则整层关闭。 公网路径靠 Access JWT；局域网仍用 #token= / 头。 限速为进程内状态机（连续失败阈值、指数退避、锁定）；重启清零可作为误锁逃生口。限速只挡鉴权门口，不限制已鉴权操作面。 设备 TOFU 与反代陷阱 设备指纹审批按 socket 直连 peer 的 IP 判定「是否本机」。Cloudflare 隧道在 localhost:3000 落地时连接显成 127.0.0.1，但公网已由 Access 兜底，可接受。 危险 若换成任意在本机落地的其他反代（nginx/Caddy 的 proxy_pass localhost、SSH 转发、frp 等），所有客户端都会显成本机，设备审批层被跳过，防线只剩 AUTH_TOKEN。此类拓扑必须使用强 token，并优先考虑 Access。UI 体检 DEVICE_GATE 会对「隧道/反代跳过设备门」类配置告警。 本机 127.0.0.1 / ::1 / localhost：视为本机，可直接 approved。 真实 LAN IP：触发 TOFU pending，须机主批准（UI 或 node scripts/device.js approve ）。 审批与日志 工具审批台账写入 approval-requests.json；进程内回调才是执行真相。重启后孤儿 pending 全部标过期（fail-closed）。 敏感写盘走 owner-only（0600）与串行 writer。 日志脱敏（token 掩码）；前端错误可上报服务端。 明确不做 多租户 / 账号体系 / 按用户隔离 workspace。 把 CCM 当 hosted SaaS 中转他人密钥。 在无 token 时监听全接口地址。 安全报告 请勿通过公开 issue 报告漏洞。走 GitHub Security Advisories（见 SECURITY.md）。 附注 默认超时与安全正交但常被问到：idleTimeoutMs 默认 600000；instanceIdleReclaimMs 默认 1800000。数据目录默认 CCM_DATA_DIR 指向项目下 data/，与 CLI 的 ~/.claude 分离。"},{"url":"pages/single-driver.html","title":"单驾驶员模型","part":"第三部分 · 方法论","lead":"Web 与 CLI 不可同时写同一会话；镜像只读追平。","text":"同一 sessionId 在同一时刻只允许一个驾驶员写入。Web 与本机 CLI 不能并行写同一会话；后到的一方只读镜像或置换冷读，避免 transcript 分叉。 为什么需要单驾驶员 会话内容的权威落点是 CLI transcript（jsonl）。若 Web 侧 SDK 子进程与终端里正在跑的 CLI 同时向同一 sessionId 追加轮次，会出现双 parent 链、模型上下文滞后、手机与终端各说各话。单驾驶员把「谁可以 send」收成互斥问题，而不是事后合并冲突。 CLI 驾 Web 只读镜像 追平磁盘 transcript，输入锁为只读。 Web 驾 SDK 长驻 query 正常发送、审批、中断。 外部脏 externalDirty 磁盘已超前于 SDK 内存，发送前须置换。 三种形态 形态 写入者 Web 行为 Web 驾驶 本进程 AgentSession 正常发送；状态栏取 SDK CLI 驾驶（只读镜像） 终端 CLI 输入锁定；catch-up 追 transcript；可选 statusline bridge 接管 Web 在镜像下成功 send 清 mirrorReadonly；dispose+resume 后 Web 驾驶 镜像机制 catchUpTick 轮询当前查看会话的 jsonl 增量 → history_append；只读时约 1s 一拍，常态约 2.5s mirror_state 广播只读锁与观察态（model/permission 等） mirrorEntryLock 切入会话时预锁（尾部 pending） mirrorReleaseStep 安静约 12.5s 后自动释放只读锁（文件仍增长则 keepAlive） cli-mirror-state 只读观察 CLI 落盘偏好，不写回 Web 实例偏好 forInstanceId 守卫 setMirror / catchUp 写入带 viewing 快照；切视图后旧 tick 不得改全局锁 clearMirrorOnViewChange 切会话/视图立即 force 清全局 mirror，防 A 会话锁套到 B（跨工作区误锁） 建议 2026-07-20 修复点：镜像误重锁（解锁后不该被旧逻辑再次锁上）； catchUpTickOnce 两分支的状态提交延后到视图守卫之后，避免切 tab 后旧 tick 写脏全局 mirror / catchUp 状态。 附注 Web resume 是 冷读 transcript，不能 attach 正在运行的 CLI 活进程。正在终端里跑的会话，Web 侧只能镜像或等其结束后再 resume。 留意 长工具若长时间不写盘，仅靠「文件增长」会误判静默；实现用 tailPending / keepAlive 等互补判据，避免横幅误灭成「感觉没在跑」。 externalDirty 与发送 catch-up 或 rebaseline 发现磁盘已超前 → 标记 externalDirty 。 用户发送时若实例 idle ：dispose + resume，冷读磁盘后再发（吸收终端写入）。 若实例 busy ：硬拒绝（NACK），可重试；不是静默排队。 externalDirty 只约束「下次 Web 写入前必须吸收磁盘」；它不单独把 statusline 锁到 CLI——statusline 是否走 CLI 快照看 mirrorReadonly 。 flowchart LR A[\"查看会话\"] --> B{\"尾部/锁判定\"} B -->|\"CLI 在写\"| C[\"mirrorReadonly\"] C --> D[\"catchUpTick 追平\"] D --> E[\"mirrorReleaseStep ~12.5s\"] E -->|\"静默满\"| F[\"解锁可输入\"] B -->|\"外部已超前\"| G[\"externalDirty\"] G --> H{\"发送时 idle?\"} H -->|是| I[\"dispose + resume 冷读\"] H -->|否| J[\"NACK\"] 切回运行中会话（busy reseed） 从其他标签切回仍在跑的会话时，前端按 server 权威 state 重种运行指示，避免「其实还在跑，UI 却像空闲」。权威来源是 instances 等载荷中的 state，而非本地 spinner 记忆。 已知边界 mirror 全局单值（非 per-连接）；切视图会清理，设计上未做细粒度 per-socket 锁（AD-5 有意保留）。 catchUp 主要盯 当前查看 会话；后台 tab 被外部写过、切入后首个 tick 前极速发送，边界上可能不经 dirty 置换（已知）。 busy→idle 窗口内部分终端写入可能被吸收而不标脏（有意取舍，防重复气泡）。 环形事件缓冲 2000 与历史消息 2000 上限是不同轴（事件信封 vs transcript 条数）。"},{"url":"pages/contracts-gates.html","title":"契约与工程门禁","part":"第三部分 · 方法论","lead":"事件契约、import 边界、inventory、check 流水线。","text":"契约与门禁把「脑子里的分层约定」变成 npm run check 硬闸：事件名与类型不许漂移，import 不许缠死，仓库清单不许悄悄回堆。 npm run check 组成 步骤 职责 ESLint 语法与基础风格、死代码、未定义引用 check-import-boundaries 分层边界 + 零循环依赖 contract-check 出向事件类型与入向 socket 事件双向保真 doc-consistency 残留文档/注释与约定一致性抽查 visual mock registry 视觉 mock 场景注册面 ⊆ 契约 playwright forbidden 禁止脆弱选择器/反模式 inventory:check 仓库文件分类与生成物不过期 import 边界：BOUNDARY_RULES 扫描根为 src/、public/js/ 与 server.js。五条规则： 规则名 不变量 frontend-no-backend public/js 不得 import src/ backend-no-frontend src/、server.js 不得 import public/js（共享白名单除外） shared-is-leaf src/shared 不得反向 import 其他后端域 server-is-sink 只有 server.js 与 src/server 自身可 import src/server runtime-no-tooling 运行时源码不得 import scripts/ 或 tests/ 另做 零循环依赖 检测（含动态 import() 扫描，不能靠动态 import 绕边界）。 建议 新增「前后端都要引用」的逻辑时，优先放进已有白名单文件的纯函数，或先扩展门禁白名单并写清理由；不要让 public/js 直接 import src/。 附注 tests/ 与 scripts/ 不设 import 边界（工具与测试可跨域引用），但运行时源码不得反向依赖它们（runtime-no-tooling）。 事件契约 权威列表在 scripts/agent-event-contract.js，由 contract-check 与源码/前端/mock 注册面交叉比对。 出向 ：agent:event 的 type 必须在 AGENT_EVENT_TYPES allowlist（真实路径与 visual mock 对照）。 入向 ：客户端 emit 的 socket 事件名必须在 INBOUND_SOCKET_EVENTS；server 注册面与契约双向相等。 前端 emit 面 ⊆ 契约；visual mock ⊆ 契约。漏注册或多注册都会红。 完整列表见 事件与端点参考 。 Inventory inventory:update node scripts/repo-inventory.js --write，有意增删文件后更新清单与 docs/repository-map.md inventory:check node scripts/repo-inventory.js --check，拒绝未分类文件与过期生成物 新文件进入仓库前须能归类；审计笔记、一次性提案等不应悄悄回到主树。check 红时先看是契约漂移还是 inventory 未更新。 建议 推送带 CI 的仓库前，可用 CI=true npm test 模拟 CI 行为。"},{"url":"pages/glossary.html","title":"术语表","part":"第三部分 · 方法论","lead":"instance、envelope、mirror、TOFU 等高频词。","text":"高频专有名词的准确定义。阅读实现章节前建议扫一遍。 术语 定义 instanceId 进程内 AgentSession 实例 ID（如 inst_N ）。换实例会变。 sessionId Claude CLI 会话 ID，与 transcript 文件名对应。 epoch 实例世代号。新实例更大；客户端见变化应重置 seq 去重基线。 seq 实例内事件序号，单调递增；用于 sync:since 补发。 envelope agent:event 外层： {seq,epoch,sessionId,instanceId,cwd,ts,type,payload} 。 AgentSession src/agent/agent.js 中长驻 SDK query 的运行时对象。 viewingInstanceId 当前「正在看」的实例；影响高频 delta 是否广播。 mirrorReadonly CLI 驾驶时的 Web 只读锁，锁定输入。 externalDirty 捕获到终端对 transcript 的外部写入后的脏标记。 FRESH 无 resumeId 的新开会话；不继承 cwd 末实例档位。 resume 以已有 sessionId 冷读/续接 transcript。 TOFU Trust On First Use：设备首次须本机批准。 CCM_DATA_DIR 控制面数据根（sessions/devices/audit/push 等）。 WORK_DIR 默认工作目录（claude cwd）。 settingSources SDK 加载 settings 的来源：user / project / local。 canUseTool SDK 权限回调；未放行则弹 permission_request 。 permission_request 需用户批准的工具调用事件。 sync:since 客户端按 lastSeq 请求缓冲回放。 catchUp 轮询 jsonl 增量，镜像 CLI 写入。 ring buffer 每实例事件环形缓冲，cap 2000 。 worktree sessions 发现 git worktree 内会话，使 Web 触达「cd 进 worktree 即 resume」的等价物。 statusline bridge 可选包装：CLI 驾驶时把 CLI statusline 快照同步给 Web。 effort 思考强度；spawn 时注入 SDK options。Web 切档经 user:setEffort → dispose+resume，不可 in-place 改活进程。 auto （权限档） 六合法档之一：SDK 模型分类器自动批/拒；另五档 default / plan / acceptEdits / bypassPermissions / dontAsk。 CLI doctor vs UI 体检 CLI： scripts/doctor.js 固定 D1–D11。UI： doctor-runtime.js 另含 Access/设备门/VAPID/白名单等，勿混为一谈。 pendingTurns 在途轮计数；仅 send 与 result/中断/撤回结算。禁止双扣；封顶配合 cliQueued。 cliQueued 已交 CLI 内部队列的一条消息登记；撤回可能走 cancelAsyncMessage。 bgTasks 活后台任务表；多子代理并行时刷新活动、防 idle 误杀；stopTask 只停单个后台任务。 视图守卫 写路径 ack / catchUp 提交前校验 viewing 快照未变；防切 tab 后旧响应污染新会话。 pendingSendBusy 前端发送忙碌锁；须按会话隔离，禁止跨会话污染。 _raceControlRequest control_request 超时赛跑；覆盖 interrupt/setModel/setPermissionMode/stopTask/cancelAsyncMessage。 附注 事实基线探针 dev@2de707b。契约事件数：入向 32、出向 25——以 scripts/agent-event-contract.js 为准。"},{"url":"pages/runtime-assembly.html","title":"启动与组装根","part":"第四部分 · 核心实现","lead":"server.js 薄入口与 app.js 组装根的启动时序。","text":"启动路径刻意拆成薄入口与组装根：环境必须在状态模块求值前就绪；listen 前必须清孤儿审批。 server.js（薄入口） 约 9 行。LaunchAgent / systemd / npm start / 集成测试都走这里。 环境加载要点 dotenv 在动态 import app 之前执行，保证 CCM_DATA_DIR 等在模块求值期已正确（状态模块在 import 时解析路径）。 空串环境变量在加载前删除（除 ANTHROPIC_*），避免 LaunchAgent 的 AUTH_TOKEN= 挡住 .env。 ANTHROPIC_* 只认启动 shell 中已有的真实值；写入 .env 的会被剥除。 parseServerConfig：port / authToken / idleTimeoutMs / instanceIdleReclaimMs / approvalTtlMs / notifyThrottleMs / sessionDeleteQuietMs / devMode / workDir / dataDir。 app.js 组装顺序 顶层 import 完成（agent / auth / sessions / files / ops / server 子模块）。 initCfAccess()；parseServerConfig 得到 port/token/timeouts/WORK_DIR/DATA_DIR。 通知通道 createNotifyChannels（Web Push + ntfy，尽力而为）。 preflight（致命则 process.exit(1)）： WORK_DIR 存在且为目录 → realpathSync 构建 workDirs 白名单（WORK_DIRS_FILE 优先，否则 WORK_DIRS） 解析 CLAUDE_BIN 或 which claude，文件须存在 采集 versions（非致命）；无 ANTHROPIC_AUTH_TOKEN/API_KEY 仅 warn 初始化 viewing 态：viewingInstanceId=null，viewingCwd=WORK_DIR（ 不自动 resume ）。 Express：configureHttpShell（压缩、CSP、asset ?v= 哈希、静态 public/）。 HTTP 鉴权 createHttpAuth + 运维路由 /health /metrics /push/*。 createServer + Socket.IO；createDeviceGate；io.use 握手鉴权。 createInstanceManager + 连接 handler（业务 on + 文件 handler）。 可选 WORK_DIRS_FILE 时 fs.watch 热加载。 进程兜底 uncaughtException / unhandledRejection（只记不退）。 listen 前：expireOrphanedPending + startApprovalRetentionSweep。 listen(port, AUTH_TOKEN ? '0.0.0.0' : '127.0.0.1')；EADDRINUSE → 可读错误 + exit(1)。 SIGINT/SIGTERM：flush sessions/approvals/audit → dispose 全部 agent → io.close → exit。 附注 app.js 是有意保留的组装根本体：mirror / catchUp / openInstance 与契约路由共享顶层可变状态（viewing*、mirror*、catchUp*）。域逻辑已下沉，接线不拆成多文件穿针。边界守卫：server 是 sink。 留意 无 AUTH_TOKEN 时只绑定 127.0.0.1，手机与隧道均不可达。这是有意设计，不是故障。 默认超时（parseServerConfig） 项 默认 PORT 3000 IDLE_TIMEOUT_MS 600000（10 min） INSTANCE_IDLE_RECLAIM_MS 1800000（30 min；0=关） APPROVAL_TTL_MS 1800000 NOTIFY_THROTTLE_MS 60000 SESSION_DELETE_QUIET_MS 300000 关键模块入口 config.js dotenv + parseServerConfig http.js HTTP 壳、鉴权中间件、运维路由 socket.js 事件注册器、设备门、ack 兜底 instance-manager.js agents Map 与 stateOf instance-routing.js 路由、externalDirty NACK 等纯函数"},{"url":"pages/agent-session.html","title":"AgentSession","part":"第四部分 · 核心实现","lead":"长驻 SDK query、权限闸、环形缓冲、排队与中断。","text":"AgentSession （ src/agent/agent.js ）是长驻 SDK query 的运行时核心：输入队列、权限闸、事件映射与环形缓冲。 职责 持有一条到 Agent SDK 的 streaming input 会话（可 resume）。 将 SDK 消息 map() 为契约事件并发出信封。 通过 canUseTool 实现权限挂起/放行；维护 pending 审批与提问。 支持排队、中断、撤回排队消息、切模型 / 权限档；effort 切档由 server 置换实例完成。 输入与忙碌 queue streaming input 队列；busy 时可排队后续消息 pendingTurns 在途轮次计数；仅由 send(+1) 与 result/中断/撤回路径结算(-1)。封顶语义：≥2 拒再入队 cliQueued 已交给 SDK/CLI、可能仍在 CLI 内部队列的一条登记（上限 1；与 pendingTurns 配合） isBusy pendingTurns 0 或有后台任务（bgTasks）或有 pending 审批/提问 idle hang 有 pendingTurns、无审批/提问、且无活 bgTasks 时，静默超过 idleTimeoutMs （默认 600000）则 abort 实例状态（instance-manager）：permission busy aborted/error/done idle。 留意 pendingTurns 禁止双扣：中断路径与撤回路径若各自对同一条消息减一次，会制造假性空闲窗口。2026-07-20 已修：谁先结算谁摘牌；cancelQueued 成功须补扣（该消息永不产生 result）。 SDK 选项要点 cwd 、 pathToClaudeCodeExecutable 、 resume 、 permissionMode 、 effort settingSources: ['user','project','local'] canUseTool 统一闸门 不 注入 allowedTools / disallowedTools 事件发出 emit(type, payload) ：分配 seq ，写入 cap= 2000 的环形缓冲，回调 server onEvent 。 emitTransient ：不占 seq、不进缓冲（如 task_progress ）。 信封字段： seq, epoch, sessionId, instanceId, cwd, ts, type, payload 。 map() 映射（摘要） SDK 侧 契约事件 流式文本 / 思考 text_delta / thinking_delta 工具调用 / 结果 tool_use / tool_result 白名单外工具 permission_request （挂起） AskUserQuestion question 轮次结束 / 初始化 result / init 压缩上下文等 system 权限闸 canUseTool 合法档六值： default / plan / acceptEdits / bypassPermissions / dontAsk / auto 。 dontAsk → deny（含 SDK 误调时的防御纵深）； bypassPermissions → 实例记真实 bypass，给 SDK 映射为 default ，由 handleCanUseTool 直接 allow。 AskUserQuestion 走 question 分支，不是普通 tool 审批卡片。 其余：算 fingerprint，写入 pending + 台账，emit permission_request ，等待 user:approve 。 allow 时校验客户端回传 op 指纹，失败则 integrity_mismatch 。 TTL（ approvalTtlMs ，默认 1800000）到期自动 deny。 进程重启：磁盘 pending 全部 expired（回调已死），fail-closed。 sequenceDiagram participant CLI as Claude CLI/SDK participant AS as AgentSession participant S as Server participant UI as Web CLI->>AS: canUseTool AS->>S: permission_request S->>UI: agent:event UI->>S: user:approve S->>AS: resolvePermission AS->>CLI: resolve behavior AS->>UI: request_resolved 排队、中断与撤回 busy 时再发可排队；system 提示「前面还有消息在排队…」；emit 在 pendingTurns++ 之前，用 queued: pendingTurns≥1 标排队可见性。 user:interrupt ：中断当前；kind interrupted ；排队可能 queue_dropped 。 user:cancelQueued ：撤回排队中的消息；kind queue_cancelled ，可带回 clientMessageId。若消息已在 CLI 侧（cliQueued），走 cancelAsyncMessage 。 勿靠中文文案字符串匹配终态，用 kind 字段。 control_request 超时保护 SDK 的 control_request 与主消息通道共享底层连接。限流重"},{"url":"pages/events-sync.html","title":"事件信封与同步","part":"第四部分 · 核心实现","lead":"agent:event 信封、seq/epoch、sync:since、catch-up。","text":"同步的单位是带序号的事件信封，而不是「整页 HTML」。客户端靠 seq / epoch 去重与补洞；磁盘侧另有 catch-up 追平 CLI 写入。 信封 seq ：实例内单调递增；用于 sync:since 。 epoch ：实例世代；换实例（effort 的 dispose+resume、dirty 置换、reclaim 等）后变大，客户端必须重置 lastSeq 基线。 入环形缓冲 cap= 2000 （ BUFFER_CAP ）；溢出丢最旧并可能标 gap。 emitTransient ：不占 seq、不进缓冲（如 task_progress 心跳）。 广播策略 默认发往已批准设备房间 approved （ io.to('approved').emit('agent:event', …) ）。 高频 text_delta / thinking_delta ：仅当该实例是当前 viewingInstanceId 时广播；后台 tab 仍写入缓冲，切回后用 sync:since 回放。 边界事件驱动角标 latch、推送节流、会话活动 touch。 建议 调试「后台会话像没字」时，先确认是否仅靠 live 流、是否切回后触发了 sync；后台不播高频 delta 是带宽设计，不是丢失。 视图守卫与 ack（2026-07-20） server 对 send / cancelQueued 等写路径的 ack 须校验当前 viewing：切会话后不得把旧会话的 ack/错误刷到新 tab。 前端 pendingSendBusy 等发送锁须按会话隔离，禁止跨会话污染（A 在忙导致 B 误显示忙碌）。 message-dedup：clientMessageId 幂等提交须防 TOCTOU（检查与 commit 之间并发重复发送）。 catchUpTickOnce ：两分支（有会话 / 无会话）写全局 catchUp/mirror 状态时，须在视图守卫通过之后再提交，防止旧 tick 写脏新 viewing。 sync:since 客户端发送 { sessionId, lastSeq, instanceId } 。 服务端 eventsSince(lastSeq) ：过滤已 resolve 的审批/提问（避免重弹）。 若缓冲被 trim 且存在空洞 → 标记 gap 并提示。 ack 可含 pending 快照（重建审批卡片）与 diskLen （对账终端写入量）。 epoch 变化 = 新实例 → 客户端重置 lastSeq，不可混接旧流。 留意 已 resolve 的审批不会从缓冲回放——必须以 pending 快照为准。极长轮次可能 gap，需依赖 history 全量或提示用户。 与 catch-up / mirror 的分工 机制 数据源 用途 环形缓冲 + sync:since 进程内存信封 Web 驾驶时的断线补发、切 tab 回放 catchUpTick / history_append 磁盘 jsonl CLI 驾驶时只读追平；标 externalDirty session:history 磁盘 jsonl 冷读 打开会话时全量/分页历史 详见 单驾驶员模型 。 出向 type 分组 组 type 流式内容 text_delta thinking_delta result 工具 tool_use tool_result 审批/提问 permission_request question request_resolved 会话控制 init system error user_message 状态 status_line permission_mode effort_mode models mirror_state instances 任务 task_notification task_progress api_retry 设备/日志 device_status pending_devices session_log history_append 完整 allowlist：出向 25 type、入向 32 事件——见 事件与端点参考 。契约由 npm run check 中的 contract-check 硬闸；与源码冲突时以 scripts/agent-event-contract.js 为准。"},{"url":"pages/sessions-history.html","title":"会话与历史","part":"第四部分 · 核心实现","lead":"sessions.json、transcript 冷读、worktree 会话发现。","text":"会话有两层真相：Web 元数据在 sessions.json ；消息正文在 CLI transcript jsonl。混淆两者是接手时最常见的误操作来源。 双真相源 层 路径 内容 元数据 SoT $CCM_DATA_DIR/sessions.json currentByCwd、标题、model/effort/permissionMode、隐藏列表、活动时间等 消息 SoT ~/.claude/projects/ encoded-cwd / sessionId .jsonl 完整 transcript；与 CLI /resume 同源 以代码为准 改 sessions.json 不会改聊天内容。备份 CCM_DATA_DIR 不会带走对话正文；清仓库 data/ 可能丢设备信任与指针，但 CLI 历史仍在。 列表与冷读 列表优先 SDK listSessions ，失败回落扫盘；结果可与 sessions.json 元数据增强合并。 历史回显走鉴权 socket 事件 session:history ， 不开 无鉴权 HTTP 数据端点。 冷读上限： HISTORY_MAX_MESSAGES = 2000 。 CLI 系统噪音行经过滤（isMeta / 系统行启发式），避免污染镜像与尾部判定（如镜像锁释放、尾部 pending）。 resume 时可冷读会话末条 assistant 模型作 chip 回落展示（不写入 activeModel 以免误重置）。 留意 getSessionMessages 返回的是清洗过的 message，不是 JSONL 原行。同步引擎（catch-up / mirror）必须读 jsonl 路径，不能用该 API 当事实源。 打开路径：FRESH vs resume 路径 触发 档位来源要点 FRESH session:new ；空首页首条消息无 resumeId 不继承 cwd 末实例 permission/effort；来自 CLI settings 默认与 UI pending resume 列表切入；懒开有 saved.id sessions.json → transcript 末 mode/model → 继承逻辑；先 prepareSessionForWebResume 释放 CLI bg 锁 启动时 不自动 resume ；空首页手选会话。 懒开：首条消息且无 live 实例时 dedupedResume （single-flight）。 idle reclaim：完全空闲超过 INSTANCE_IDLE_RECLAIM_MS （默认 1800000，0=关）可回收子进程；盘上 transcript 保留，再发会 resume。 删除分层 L1 ：hide（写入隐藏列表），transcript 仍在，列表不可见。 L2 永久删除 ：动文件；受安静期等守卫（ SESSION_DELETE_QUIET_MS 默认 300000）保护，避免误删终端仍在用的会话。 worktree 会话 worktree-sessions.js 发现 git worktree 内会话，使 Web 可触达「在 worktree 里跑的 CLI 会话」——终端「cd 进 worktree 即 /resume」的 web 等价方向。实现与 knownWorktrees 注册（仅服务端以 git worktree list 权威写入）、列表分组相关。 合法 cwd = 白名单目录本身，或已注册 linked worktree 且所属 repo 仍在白名单（repo 热移除 → worktree 立即失效）。 SDK listSessions 的 dir 匹配含祖先目录——查 worktree 会混入父仓会话；代码已加 jsonl 归属过滤 ，只保留真正属于该 worktree 路径的会话。 2026-07-20：首页「最近活跃」纳入 worktree 会话，冷启动可一键续（前端 logic + app 列表合并）。 关键模块 sessions.js 元数据读写、currentByCwd、隐藏 history.js jsonl 冷读、catchUp、mirror 状态机、尾部分类 workdirs.js 白名单与 sessionLimit worktree-sessions.js worktree 会话发现 attention.js 「需要你」类聚合"},{"url":"pages/auth-device.html","title":"鉴权与设备信赖","part":"第四部分 · 核心实现","lead":"AUTH_TOKEN、CF Access、限速、设备指纹 TOFU。","text":"鉴权是纵深结构：token（或 Access JWT）证明「知道密钥」；设备 TOFU 证明「这台设备被主人批过」。合法 token alone 不足以操作未信任设备上的业务。 绑定与可达性 条件 listen host 可达性 AUTH_TOKEN 非空 0.0.0.0 LAN + 隧道可达 未设 / 空串 127.0.0.1 仅本机；手机与隧道均不可达（有意） 代码： src/server/app.js 中 host = AUTH_TOKEN ? '0.0.0.0' : '127.0.0.1' 。 HTTP 与 Socket 共用 Token 匹配：HTTP 用头 x-auth-token 或查询 ?token= ；Socket 用握手 auth。 限速 Map 共享： /health 、 /metrics 、 /push/* 与 socket 握手共用，防无限试 token（默认连续失败阈值与锁定逻辑在 rate-limiter.js ）。 公网 Host 且 Access 启用时：优先 JWT；LAN / 其它 Host 用 token。 无鉴权门（无 token 且非公网 Host）时限速不激活：本机调试路径 auth 恒过。 flowchart TD Conn[\"新连接\"] --> Pub{\"公网 Host\\n且 Access 已配?\"} Pub -->|是| JWT[\"校验 CF Access JWT\\nfail-closed\"] Pub -->|否| Tok{\"AUTH_TOKEN\\n匹配?\"} JWT --> RL[\"限速 onAuthResult\"] Tok -->|否| Deny[\"拒绝\"] Tok -->|是| RL RL --> Dev{\"本机或已信任\\n或 Access?\"} Dev -->|否| Pending[\"TOFU pending\"] Dev -->|是| OK[\"进入业务房间 approved\"] Cloudflare Access CF_ACCESS_HOSTNAME 、 CF_ACCESS_TEAM 、 CF_ACCESS_AUD 三项齐全 才启用。校验经 jose + JWKS（缓存 cf-access-certs.json ），失败则拒绝。任一缺失则整层关闭，回退 token 路径。改 env 后须重启常驻 server。 留意 OTP 已过但 app 仍连不上：多为 JWT 校验失败（TEAM/AUD 与 CF 应用不一致）。日志搜「Access JWT」。 设备信赖（TOFU） 文件 内容 $CCM_DATA_DIR/trusted-devices.json 已信任设备（含 deviceToken） $CCM_DATA_DIR/pending-devices.json 等待批准的设备请求 本机 loopback（ 127.0.0.1 / ::1 / localhost ）可跳过设备门。 非本机须批准后才能进入业务房间 approved 。 CLI： node scripts/device.js list|approve ID |deny ID （与 server 读同一 CCM_DATA_DIR ）。 待批通过 pending_devices 等事件推到已信任端；UI 亦可批准。 危险 任意把公网流量反代到 127.0.0.1 的拓扑（nginx/Caddy 的 proxy_pass localhost 、SSH 转发、frp 等），都会让 peer 显「本机」，从而跳过设备审批，防线只剩 token（+ 若开 Access 则 JWT）。Cloudflare 命名隧道落到 localhost 时同样显本机，但公网有 Access 兜底，本配方可接受。 与审批台账的区别 设备 TOFU 管「谁可以连上来操作」；工具 permission_request 管「CLI 是否执行某条命令」。二者都是 fail-closed 纵深，不可互相替代。重启后设备信任仍在磁盘；在途工具审批回调死亡，磁盘 pending 会标过期。 实现： src/auth/* 、 src/server/http.js 、 src/server/app.js 握手段。"},{"url":"pages/files-upload.html","title":"文件与上传","part":"第四部分 · 核心实现","lead":"工作区白名单、三层路径闸、附件与预览。","text":"文件能力围绕「白名单工作区」与「只读不越界」：上传附件进会话，浏览与预览不得逃出允许目录。 工作区白名单 默认 cwd： WORK_DIR （未设则 home）。 附加目录： WORK_DIRS （逗号串）或 WORK_DIRS_FILE （JSON 文件，优先）。 WORK_DIRS_FILE 支持热加载：server 监听文件变更（父目录 watch + mtime + debounce）；改完免重启。 被移除目录上 已有 live 实例可继续 ；该目录上 新开会话拒绝 （ ensureWhitelisted ）。 无效目录 soft-skip 并告警，不挡启动。 以代码为准 口语常说「改 workdirs.json」。契约环境变量名是 WORK_DIRS_FILE （值通常指向仓库根 workdirs.json ）。未设置该 env 时不会 watch，改文件也不热加载。 附件上传流程 客户端经 socket 上传字节（校验类型与大小，见 validateAttachments ）。 服务端写入 当前 cwd /.ccm-uploads/ ts - rand - safeName （常量 UPLOAD_DIR = '.ccm-uploads' ）。 buildPromptText 把绝对路径注入 prompt，供 CLI / 工具 Read 。 事件 meta 带缩略信息与 storedName ；历史消息可按需再拉预览（browse 读盘，非无鉴权 HTTP 图片端点）。 留意 transcript 记录的是当时绝对路径。挪工作区、改机器用户名或清空 .ccm-uploads 后，旧附件可能无法打开。备份对话若仍要看图，须一并备份对应上传目录。 路径闸（三层） 层 模块 作用 写安全 file-security.js owner-only 写（0600）、符号链接危险组件拒绝、原子写辅助 范围门 workdir-scope-guard.js 路径必须落在白名单 realpath 之下 浏览/预览 file-browse.js · file-preview.js 只读列表、片段与 diff；工具卡片同源约束 工具卡片对 Edit / Write 显示 diff、对 Read 显示片段，均限白名单工作目录内，只读不越界。 相关 socket 事件 上传随 user:message 附件字段进入主发送路径。 browse:list / browse:read ：工作区内只读浏览（含历史附件按需读）。 tool:preview / tool:full ：工具结果预览与全文拉取。 实现入口： src/files/* 、 src/server/socket-files.js 、 src/sessions/workdirs.js 。"},{"url":"pages/frontend-pwa.html","title":"前端与 PWA","part":"第四部分 · 核心实现","lead":"public/ 分层、Socket 客户端、可安装 PWA。","text":"前端是原生 ESM 的单页 PWA：Express 直接托管 public/ ，无打包步骤、无 CDN 运行时依赖。 分层 路径 角色 public/index.html 壳、资源引用、PWA meta public/css/app.css 样式（配合 Tailwind 运行时配置） public/js/app.js 编排：socket、UI 状态、子模块装配（大壳） public/js/logic.js 纯决策函数（禁 DOM / socket）；可被 node:test 直接测 public/js/canonicalize.js 操作规范化；与后端共享白名单文件 public/js/ring-buffer.js 客户端侧缓冲辅助 public/js/app/* 会话、附件、审批、设置、通知、同步、渲染、任务状态等控制器 public/js/sw.js · sw-cleanup.js Service Worker 与清理 public/vendor/ Tailwind / marked / highlight / DOMPurify / 字体 本地自托管 manifest.webmanifest · icons/ 可安装 PWA 与 maskable 图标 建议 logic.js 红线：只做数据→数据。副作用（DOM、socket、localStorage 写）放在 app/* 控制器。单测优先覆盖 pure 函数。 连接与 token 首次通过 URL hash #token=… 注入鉴权密钥。 写入 localStorage 后从地址栏清除，避免分享 URL 泄露。 设备 token 同样持久化，参与 TOFU 握手。 Socket 连接后进入事件分发（ event-dispatch 等）；按 viewingInstanceId 分流。 非当前 viewing 实例的高频 text_delta / thinking_delta 可能不直播到该 tab；切回后靠 sync:since 回放缓冲。 重连后携带 lastSeq / epoch；epoch 变化时重置去重基线。 发送忙碌锁（pendingSendBusy 等）与消息去重须 按会话隔离 ；切会话后不得沿用上一会话的 busy / 重复 clientMessageId 判定。 server ack 受视图守卫：切 tab 后旧 send/cancelQueued 的回包不应驱动当前 composer 状态。 限流重试中点「停止」：control_request 超时后应收口「正在停止…」，勿永久卡住（2026-07-20）。 首页「最近活跃」含 worktree 会话，可冷启动一键续。 PWA 与推送前提 manifest.webmanifest + maskable 图标，支持「添加到主屏幕」独立显示。 iOS Web Push：需先安装到主屏幕且 https；局域网纯 http 场景 Web Push 不可用 → 配 ntfy。 Service Worker 负责推送相关与缓存策略（实现以 sw.js 为准）。 与边界守卫的关系 public/js 不得 import src/ 。允许共享的仅白名单： public/js/canonicalize.js （后端 fingerprint 等使用） public/js/logic.js （允许列表中；后端当前实际主要用 canonicalize，见 漂移清单 ） 测试方式 E2E 使用 Playwright 移动视口 + 仓库内 mock server（零外部 token）。断言基于 DOM 状态，非像素比对。场景注册受 visual mock registry 门禁约束，mock 发出的 agent:event type 不得超出契约。 inventory 中前端源约 21 文件、vendor 约 10、图标为生成资产（ scripts/gen-icons.js ）。"},{"url":"pages/notify-status.html","title":"通知与状态栏","part":"第四部分 · 核心实现","lead":"Web Push / ntfy、statusline 与 CLI bridge。","text":"通知与状态栏都是「尽力增强」：失败不阻断主对话；Web 驾驶与 CLI 驾驶的状态来源不同。 Web Push 需 VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY / VAPID_SUBJECT 三项齐全，否则优雅缺席。 订阅保存在 $CCM_DATA_DIR/push-subscription.json 。 HTTP： GET /push/vapid-public-key 、 POST /push/subscribe （均走 httpAuth）。 典型触发：审批、提问、结果类（经分类与节流）。 实现： src/ops/notify-channels.js + notifications.js 。 留意 iOS 上 Web Push 要求「添加到主屏幕」且 https。局域网 http 不可用；此时应配 ntfy 或保持页面前台。 ntfy NTFY_URL + NTFY_TOPIC 启用；可选 NTFY_TOKEN 访问私有实例。 PUBLIC_URL 用于通知深链绝对 URL；空则回退 https://$CF_ACCESS_HOSTNAME 。 server 一行 POST 到 ntfy；手机装 ntfy app 订阅 topic 可锁屏收通知。 正文可能含命令 / 审批摘要 → 务必自托管或私密 topic + token，勿用公共 ntfy.sh 裸 topic。 节流窗口： NOTIFY_THROTTLE_MS 默认 60000 （同会话同类通知）。投递失败会记入服务健康（deliveryFailure），面板可展示，不阻断对话。 Statusline 来源选择 场景 来源 Web 驾驶 SDK 状态（模型、上下文、成本等） CLI 驾驶 + bridge 已装 CLI 快照目录中的 statusline 观察态 WEB_STATUSLINE=off 关闭 Web 状态栏 UI CLI_STATUSLINE_BRIDGE=off server 侧回滚为 SDK-only（不自动卸载 CLI wrapper） 安装后须重开 Claude CLI 并重启常驻 server。CLI 与 server 的 CLI_STATUSLINE_DIR 须一致（默认约定在 ~/.claude/ccm/… 下）。 建议 多数用户只需 Web 驾驶的 SDK 状态栏。仅在「Web 只读镜像正在 CLI 里跑的会话、且要看 CLI 同源 statusline」时再装 bridge。 动态状态行与回合收尾 运行中可展示 CLI 式动态状态行（动词 / 秒表 / tokens 等，经 status_line 事件）。 成功轮可有回合收尾行（对齐 CLI：过去式动词 + 时长）；中断/错误路径有意不同文案。 服务状态面板 instances 广播可带判定化字段： 面板渲染「基础 + 判定化告警」两段；告警带 24h 时效窗自动退场。 裸计数器不进入面板（对人无参照系）；原始计数留 /metrics 。 与「需要你 (N)」角标聚合是不同轴，不混判。 相关： 单驾驶员 （谁驾驶决定 statusline 源）、 运维排错 （/metrics）。"},{"url":"pages/data-store.html","title":"数据全景","part":"第五部分 · 数据与集成","lead":"CCM_DATA_DIR、transcript、上传与忽略态文件。","text":"控制面状态、CLI transcript、附件与内存态分属不同根目录。备份与清盘前，先分清「可迁出的 CCM 状态」与「CLI 真相源」。 存储全景 路径 角色 生命周期 $CCM_DATA_DIR/sessions.json 会话元数据与偏好（含隐藏列表等），非消息正文 持久；宜 0600 …/approval-requests.json 审批台账；执行闸门仍在内存 持久台账 …/audit-records.json 安全审计环形记录 持久 …/trusted-devices.json · pending-devices.json 设备 TOFU 持久；清盘 = 废掉手机信任 …/cf-access-certs.json Access JWKS 缓存 可弃；损坏再拉 …/init-cache.json 模型列表等 init 缓存 可弃 …/push-subscription.json Web Push 订阅 持久 workdirs.json （由 WORK_DIRS_FILE 指向） 多工作区白名单；可热加载 不在 CCM_DATA_DIR；勿提交 ~/.claude/projects/ 编码cwd / sessionId .jsonl 消息真相源；与 CLI /resume 同源 不随 CCM_DATA_DIR 迁走 WORK_DIR/.ccm-uploads/ 附件原件；prompt 注入绝对路径 删附件 → 历史预览失效 内存 ring buffer · agents Map 最近 2000 事件、在途状态 易失 默认根 CCM_DATA_DIR 未设时回落仓库 ./data 。生产建议外置绝对路径、目录 0700、敏感 JSON 0600。多数状态模块在 import 时解析路径，故 env 须在 server.js 薄入口加载完成后再装 app.js 。 双真相源 CCM 控制面 sessions.json 、设备、审批台账、推送、缓存。描述「web 如何看待会话与谁可连」，不替代对话正文。 CLI transcript ~/.claude/projects/**/*.jsonl 是对话与工具轨迹的权威落盘。历史冷读、镜像追平、续接都读这里。 风险 只备份 CCM_DATA_DIR 会丢掉消息正文；只备份 ~/.claude 会丢掉设备信任、隐藏会话与推送订阅。两端不一致时可能出现「有元数据、无历史」或续接失败。 备份含义 完整可恢复 ：外置 CCM_DATA_DIR + 相关 ~/.claude/projects/ + 各 WORK_DIR/.ccm-uploads/ （若仍要打开历史附件）+ .env / 隧道凭据（另管）。 可丢弃 ：init-cache、cf-access-certs、环形缓冲与 agents 内存态。 迁移 ：停常驻服务 → 拷贝数据根 → 改 CCM_DATA_DIR → node scripts/doctor.js → kickstart 重启。 热加载例外 WORK_DIRS_FILE 指向的文件由 server 监听，改完即生效、免重启；被移除目录上已开会话可继续，仅拒新开。逗号串 WORK_DIRS 无文件可 watch，改后须重启。 危险 一次性测试 server 必须隔离 PORT / WORK_DIR / CCM_DATA_DIR / AUTH_TOKEN ，否则会污染生产 data。 硬编码证据： src/sessions/history.js 中 CLAUDE_DIR = join(homedir(), '.claude', 'projects') ；deployment 文档亦写明 transcript 不迁入 CCM_DATA_DIR。"},{"url":"pages/integrations.html","title":"外部集成","part":"第五部分 · 数据与集成","lead":"Agent SDK、claude CLI、Cloudflare、推送通道。","text":"外部依赖只做「桥」与「通道」，不承接多租户账号，不替代本机 CLI。每一层都有明确的做与不做。 职责对照 集成 做什么 不做什么 Claude Agent SDK + 本机 claude CLI 长驻 query() 、流式输入、权限回调、spawn CLI；继承本机 settings / CLAUDE.md / MCP / skills / hooks 不重新实现 Claude；不注入独立 allowedTools 清单；不自带登录态 Cloudflare Tunnel + Access （jose 验 JWT） 固定域名把公网流量落到 localhost:3000 ；公网 Host 上强制 Access JWT（三项 env 齐全才启用） 不替代 AUTH_TOKEN （LAN/本机仍靠 token）；不负责业务鉴权细粒度授权 web-push （VAPID） 浏览器推送：审批、提问、结果等（需 https；iOS 先加主屏幕） 未配 VAPID 则优雅缺席；不保证息屏必达 ntfy 服务端 POST 通知，绕过 iOS PWA / 局域网 http 限制；可配深链 PUBLIC_URL 未配则优雅缺席；公共裸 topic 不安全（正文可能含命令线索） 附注 ANTHROPIC_* 只认启动 server 的 shell 环境；写进 .env 会在启动期剥除。官方订阅与第三方网关都走同一 CLI/SDK 路径，差别只在 shell 里 export 了什么。 生态关系 flowchart TB Phone[\"手机浏览器 / PWA\"] CF[\"Cloudflare 边缘\\nAccess JWT\"] Tunnel[\"cloudflared 命名隧道\"] S[\"CCM server\\nExpress + Socket.IO\"] SDK[\"Agent SDK\"] CLI[\"本机 claude CLI\"] FS[\"WORK_DIR 项目文件\"] Push[\"web-push / ntfy\"] Data[\"CCM_DATA_DIR\"] Tx[\"~/.claude/projects jsonl\"] Phone -->|\"公网\"| CF --> Tunnel --> S Phone -.->|\"同 WiFi #token\"| S S --> SDK --> CLI --> FS S --> Data CLI --> Tx S --> Push Push -.->|\"可选通知\"| Phone 运行时 npm 依赖 生产依赖（ package.json ）： @anthropic-ai/claude-agent-sdk （探针时 0.3.201） express · compression · socket.io · dotenv web-push · jose 前端第三方不经 npm 运行时路径，而是自托管到 public/vendor/ （Tailwind / marked / highlight.js / DOMPurify 等），见 public/vendor/THIRD-PARTY-NOTICES.md 。 可选与缺席 CF_ACCESS_HOSTNAME / TEAM / AUD 任一缺失：Access 整层关闭，公网与 LAN 都回退 token 模型。 VAPID 三项不全：Web Push 关闭；仅 ntfy 或无推送仍可交互（需打开页面审批）。 statusline:install 不随 npm install 自动执行；CLI bridge 显式安装后才与终端 statusLine 同步。 项目不内置 cloudflared 生命周期管理；凭据在 ~/.cloudflared ，模板在 deploy/ 。 与职责边界的衔接 集成层之上的产品边界（单用户、非 SaaS、权限继承 CLI）见 职责边界 与 安全模型 。本页只回答「外部系统各自管什么」。"},{"url":"pages/quickstart.html","title":"快速开始","part":"第六部分 · 部署与运维","lead":"安装、setup、doctor、局域网与临时隧道。","text":"最短可跑路径与 README 一致：装依赖、写 .env 、doctor、起服务。同 WiFi 必填 AUTH_TOKEN ；公网用临时隧道即可试用。 English Install path for non-Chinese readers: English Quickstart （与 README.en.md 对齐）。 页面更新 2026-07-23 · 事实基线见手册封面（dev@2de707b） 前置 Node ≥ 20（ node --version ） 本机已安装并登录的 claude CLI（ which claude ） macOS 或 Linux 克隆并确认前置。 安装运行依赖并交互配置。 自检。 启动。 如何从手机连上 场景 做法 同一 WiFi 在 .env 设非空 AUTH_TOKEN ，打开启动日志中的 http:// lan-ip :3000/#token=… 。无 token 时只绑 127.0.0.1 ，手机连不上（有意为之）。 临时公网 / PWA https 另开终端： cloudflared tunnel --url http://localhost:3000 ，手机打开随机域名并带 #token= 。token 首次写入 localStorage 后从地址栏清除。 固定生产 命名隧道 + Access 2FA + 常驻进程；见 生产部署 。 留意 ANTHROPIC_* 只认启动 server 的 shell。第三方网关请在该 shell 中 export 后再启动。写进 .env 会被剥除。官方订阅则依赖 CLI 自身登录态即可。 建议 可选 CLI 镜像状态栏： npm run statusline:install （不会随 install/start 自动装）。查看： npm run statusline:status 。多数用户只开 Web 驾驶时不需要。 开发模式 生产常驻已占 3000 时，勿再手动 npm start 撞端口；临时调试先 bootout 常驻服务。更多见 运维与排错 。"},{"url":"pages/production-deploy.html","title":"生产部署","part":"第六部分 · 部署与运维","lead":"LaunchAgent、命名隧道、Access 2FA、CCM_DATA_DIR。","text":"生产形态是两个常驻进程加固定域名：Access 守公网，隧道落到本机 loopback，server 经登录 shell 启动以继承与终端一致的 PATH 与登录态。 架构 公网入口 固定域名；Access 验 OTP / IdP；公网路径不带 #token= LAN / 本机 仍可用 http:// lan-ip :3000/#token=… ，走 AUTH_TOKEN 两个常驻进程 server（node）与 tunnel（cloudflared）；崩溃自拉起、关终端不掉 从零搭建（摘要） 隧道。 cloudflared tunnel login → create → route dns → 写 ~/.cloudflared/config.yml ingress 到 http://localhost:3000 。凭据勿提交。 Access。 Zero Trust 添加 Self-hosted 应用；取 AUD tag；填入 CF_ACCESS_HOSTNAME / TEAM / AUD 三项。 常驻。 用 deploy/*.plist.template + scripts/render-plist.js 渲染后 launchctl bootstrap （Linux 换 systemd，思路相同）。 控制面外置。 设 CCM_DATA_DIR 绝对路径 0700；doctor 再 kickstart。 可选通知。 VAPID 与/或 ntfy + PUBLIC_URL 。 LaunchAgent 模板 deploy/server.plist.template — 经 zsh -lc 'cd repo exec node server.js' 启动，保 PATH/登录态；RunAtLoad + KeepAlive deploy/tunnel.plist.template — cloudflared tunnel run name deploy/log-rotate.plist.template — 定时 scripts/rotate-logs.sh （copy-truncate） 附注 用 render-plist.js 做字面量替换与 XML 转义；路径含 / # / 空格时勿用裸 sed。 控制面外置 transcript 仍在 ~/.claude/projects/ ；附件仍在各 .ccm-uploads/ 。迁移前停服并备份，迁后 doctor 再重启。 运维铁律 生产端口已被常驻占用： 勿再 npm start （撞端口）。 改 .env 或代码后须 launchctl kickstart -k gui/$(id -u)/ label （或 systemd restart）才生效。 例外： WORK_DIRS_FILE 热加载免重启 server。 LaunchAgent 在登录后启动；开机未登录也跑需改 LaunchDaemon（进阶）。 危险 反代到 loopback 会使设备审批按「本机」跳过。本配方公网靠 Access JWT 兜底；换成其它本机反代时务必强 AUTH_TOKEN ，勿只靠设备层。详见 安全模型 。 更细的隧道 / Access / ntfy / 排错表见源码 docs/deployment.md 与 运维与排错 。"},{"url":"pages/ops-troubleshoot.html","title":"运维与排错","part":"第六部分 · 部署与运维","lead":"重启、日志轮转、/health /metrics、常见故障。","text":"常驻实例的日常操作、排错表、探针端点与 doctor 摘要。改 env 不生效，十有八九是忘了 kickstart。 运维速查 排错表 现象 处理方向 公网 502 / 1033 server 未跑：看 server 日志并 kickstart；或隧道挂了：看 tunnel 日志是否有 Registered tunnel connection OTP 过了但 app 连不上 JWT 校验失败：日志搜「Access JWT」；核对 CF_ACCESS_TEAM / AUD 与 CF 应用一致 手机进不了登录页 DNS / 隧道连通；ingress 指向 http://localhost:3000 改 env 不生效 常驻进程未重启；kickstart / systemctl restart model_not_found 模型名可能需后缀；在启动 shell export ANTHROPIC_MODEL 后重启，或 web 端 /model 切换（.env 中 ANTHROPIC_* 被剥） 部署机全局代理仍 1033 代理 TUN 劫持 cloudflared 到 edge；需给 edge 直连路由或绕过 TUN 只有工具卡片无正文 部分网关不流式；agent map 有全文兜底；仍复现则 LOG_STDERR=1 看子进程 手机连不上且无 AUTH_TOKEN 只绑 127.0.0.1——有意设计；设 token 并重启 设备一直 pending node scripts/device.js list / approve；或检查是否被反代当成「本机」而路径异常 探针与鉴权 端点 说明 GET /health 健康 JSON；经 httpAuth GET /metrics 指标快照；同样鉴权，不落盘、非 Prometheus 文本 GET /push/vapid-public-key · POST /push/subscribe Web Push；鉴权 业务历史与会话数据走 Socket（如 session:history），不开放无鉴权的 HTTP 数据端点。限速与握手共用，防无限试 token。 日志轮转 scripts/rotate-logs.sh 使用 copy-truncate ：launchd 持有 O_APPEND fd，rename 式轮转会把新文件写成空。默认超约 20MB 才转，gzip 保留 5 份。由 log-rotate.plist.template 定时触发。 诊断开关 DEBUG_SDK_MESSAGES / LOG_INTERACTIONS / LOG_STDERR 会显著放大日志；生产确认问题后关掉并重启。面板「服务状态」可看到部分开关是否打开。 doctor：两套面，勿混为一谈 CLI 自检（scripts/doctor.js · 固定 11 项） 命令行 node scripts/doctor.js 跑 D1–D11，启动前配置诊断用： # 检查 D1 AUTH_TOKEN 非空与长度 D2 CLAUDE_BIN / which claude 可执行 D3 WORK_DIR / WORK_DIRS(_FILE) 可写 D4 PORT 未被占用 D5 WEB_STATUSLINE 配置口径 D6 CLI statusline bridge 安装态（只读） D7 网关环境：.env 中 ANTHROPIC_* 会被剥除的提示 D8 .env / data JSON 权限（owner-only） D9 文档一致性（死链、旧文件名、scripts、SDK 版本） D10 前端 public/js/**/*.js 语法 D11 单测行覆盖率门槛（≥65% 未过则 warn） UI 体检（运行中 · doctor-runtime / doctor-checks） 手机/Web 面板触发的 doctor:run 走 src/ops/doctor-runtime.js ，与 CLI 11 项 不是同一清单 。UI 侧会给出脱敏安全视图，额外包含例如： CF Access 是否启用、AUD 是否配置（仅布尔） 设备门 / 反代 loopback 是否跳过 TOFU（DEVICE_GATE） Web Push VAPID 是否齐全 信任/待批设备计数 CLI settings 危险白名单摘要（WHITELIST） 排障时：启动前问题优先 CLI doctor；线上「现在这台实例安全吗」看 UI 体检与服务状态面板。"},{"url":"pages/project-rules.html","title":"项目规范","part":"第七部分 · 参考与规范","lead":"分支纪律、门禁、worktree 约定、测试分层。","text":"分支、门禁、测试分层与贡献约定。遵守这些约束，比多写一层文档更能保持仓库可合并。 分支与 worktree 日常开发在 dev ； master 为稳定 / GitHub 默认 / 分支保护。功能完成后由 dev ff 合并 master，发版用 scripts/release.sh 。 仓内 .worktrees/ 是其他分支（promo、gh-pages）的检出位， 不是本分支源码 。搜索与审查排除该目录（裸 grep / find 加 --exclude-dir=.worktrees ）。 宣传物与 landing 站留在 gh-pages / promo 孤儿线，不回灌 master/dev。 门禁 新增、移动、删除入库文件后须 npm run inventory:update ，否则 check 红。生成的 docs/repository-map.md 勿手改正文。 建议 推送带 CI 的仓库前可用 CI=true npm test 模拟 CI。集成在 CI 中常 skip 需 CLI 段；真 Claude turn 用 RUN_CLAUDE_INTEGRATION=1 。 测试分层 命令 含义 npm run test:unit 纯逻辑单测，零 token，最快 npm run test:integration 起真 server；需本机 CLI npm test unit + 可靠 integration； --test-force-exit npm run test:e2e Playwright 移动端 UI，mock server，零外部 token npm run test:smoke 真 Claude 回合；默认不跑，耗 token E2E 断言基于 DOM 状态，非像素比对。mock 发出的事件 type 必须 ⊆ 契约 allowlist。 工程约定 不引入 TypeScript 编译闸（tsc） ：项目以 Node ESM + ESLint + 契约脚本为门禁。 src/server/app.js 组装根有意保留 ：mirror / catchUp / openInstance 与契约路由共享顶层可变状态；勿为「拆文件」而拆同步引擎，除非有明确边界设计与全量回归。 commit 不加 Claude 署名 ：不要 Co-Authored-By / Claude-Session 行；说明改了什么、为何、如何验证即可。 一次性测试 server 须隔离四件套： PORT / WORK_DIR / CCM_DATA_DIR / AUTH_TOKEN ，避免污染生产 data。 生产改 .env /代码后须重启常驻进程；例外是 WORK_DIRS_FILE 热加载。 附注 并发开发同一文件时用 git worktree 隔离，避免互相 reset 冲掉未提交工作。本手册产物在 gh-pages 的 docs-site，不回写 dev 的产品 docs。 分层规则细节见 契约与工程门禁 。"},{"url":"pages/api-events.html","title":"事件与端点参考","part":"第七部分 · 参考与规范","lead":"入向 socket 事件、agent:event 类型、HTTP 运维端点。","text":"入向 socket 事件、出向 agent:event 类型，以及鉴权 HTTP 运维端点。权威清单是源码 scripts/agent-event-contract.js （探针时：入向 32 、出向 type 25 ）。历史不走开放 HTTP。 入向 Socket（客户端 → 服务端）· 32 INBOUND_SOCKET_EVENTS 完整列表（按字母序与源码 freeze 数组一致）： 分组 事件（共 32） 会话（9） session:close session:delete session:deletePermanent session:history session:home session:list session:new session:switch worktree:sessions 同步（3） conn:ping mirror:syncNow sync:since 用户控制（10） user:answer user:approve user:approveDevice user:cancelQueued user:denyDevice user:interrupt user:message user:setEffort user:setPermissionMode user:setViewing 任务 / 工具（3） task:stop tool:full tool:preview 文件浏览（2） browse:list browse:read 运维 / 诊断（5） dev:restart doctor:run logs:clientError logs:get service:status 附注 历史经 session:history ；断线补发用 sync:since 。缓冲 cap 2000。 user:setEffort 触发 dispose+resume 置换实例，不是 in-place 改活 SDK query。 建议 新增入向事件时必须改 INBOUND_SOCKET_EVENTS 与 server 注册面，否则 npm run check / contract-check 失败。本页若与源码不一致，以源码为准。 出向 agent:event 类型 · 25 AGENT_EVENT_TYPES 完整列表： 分组 type 生命周期（6） init instances result error system session_log 流式 / 历史（4） text_delta thinking_delta user_message history_append 工具（2） tool_use tool_result 权限 / 问答（4） permission_request permission_mode question request_resolved 模型 / 强度（2） models effort_mode 镜像 / 状态（3） mirror_state status_line api_retry 设备（2） device_status pending_devices 后台任务（2） task_notification task_progress 信封： { seq, epoch, sessionId, instanceId, cwd, ts, type, payload } 。其中 task_progress 等可经 emitTransient 发出（不占 seq、不进缓冲）。 HTTP 路径 说明 GET /health 健康；鉴权 GET /metrics 指标；鉴权 GET /push/vapid-public-key · POST /push/subscribe Web Push 静态 / · /js/* PWA 资源 留意 门禁要求：server 注册面与契约双向相等；前端 emit ⊆ 契约；visual mock ⊆ 契约。漏注册或多注册都会红。"},{"url":"pages/env-reference.html","title":"环境变量参考","part":"第七部分 · 参考与规范","lead":"从 .env.example 与 parseServerConfig 核对的配置表。","text":"配置以 .env.example 与 parseServerConfig / 各模块读取为准。空串在加载期等同未设置（ANTHROPIC_* 另有规则）。 变量 默认 含义 易错 AUTH_TOKEN 空 → 仅 127.0.0.1 共享密钥 LAN/公网必非空 PORT 3000 监听端口 生产勿撞常驻 CCM_DATA_DIR ./data 控制面根 生产外置绝对路径 WORK_DIR $HOME 默认 cwd 公网应钉具体项目 WORK_DIRS_FILE 空 JSON 白名单；热加载 契约名不是文件名本身 WORK_DIRS 空 逗号白名单 改后须重启 CLAUDE_BIN which claude CLI 路径 LaunchAgent 常需绝对路径 IDLE_TIMEOUT_MS 600000 静默挂死中断 过短误杀长工具 INSTANCE_IDLE_RECLAIM_MS 1800000 空闲回收；0=关 与 IDLE 正交 APPROVAL_TTL_MS 1800000 审批 TTL example 可能未列 NOTIFY_THROTTLE_MS 60000 通知节流 SESSION_DELETE_QUIET_MS 300000 永久删安静期 DEV_MODE 空 面板重启等 生产勿开 LOG_STDERR / LOG_INTERACTIONS / DEBUG_SDK_MESSAGES 空 诊断日志 生产勿长开 CF_ACCESS_HOSTNAME / TEAM / AUD 空 Access 三项全齐才启用 缺一则整层关 VAPID_* 空 Web Push 三项 私钥勿入库 NTFY_URL / TOPIC / TOKEN 空 ntfy 勿用公共裸 topic PUBLIC_URL 空→CF host 通知深链 WEB_STATUSLINE 默认开 off 关闭 CLI_STATUSLINE_BRIDGE 空 off 回滚 SDK-only 不自动卸载 wrapper CLI_STATUSLINE_DIR 约定 snapshots 路径 CLI 与 server 同值 ANTHROPIC_* 仅 shell 网关/模型 .env 无效 危险 LaunchAgent 若 export AUTH_TOKEN= 空串，会挡住 dotenv 填入。空串应视为未设。 环形缓冲 BUFFER_CAP = 2000 为代码常量，无对应 env。"},{"url":"pages/drift.html","title":"漂移清单","part":"第七部分 · 参考与规范","lead":"文档与代码不一致处，以代码为准。","text":"下列条目是文档/注释与当前代码之间已知不一致。阅读与改需求时以代码与本手册为基线。 条目 文档/注释侧 代码侧（为准） 设计长文 多处注释仍写 docs/design.md ；旧链到该文件章节 文件已删（2026-07-19）。安全权威 = README「安全模型」；运维 = docs/deployment.md repository-map 分类 生成表把大量 src/** 标成统一 domain module 边界脚本另有 sink/分层语义；map 是 inventory 用途，不区分组装根 workdirs 配置名 CLAUDE/口语常说「workdirs.json 热加载」 契约环境变量名是 WORK_DIRS_FILE （值常为仓库根 workdirs.json）；未设则回退 WORK_DIRS 且无热加载 SHARED_ALLOWLIST allowlist 含 logic.js （注释：server 与浏览器共用） 后端实际主要 import canonicalize.js ；logic 以浏览器与单测消费为主 缓冲数字 历史文档可能写 500 等 环形缓冲 2000 （BUFFER_CAP）；HISTORY_MAX_MESSAGES 亦 2000 需求来源 旧 design / demo 场景族 / 宣传脚本 7/19 起已撤出主开发线；勿按已删 design 找需求；demo 脚本需从 git 历史找回 AD-x / NFR-x 注释 代码中大量溯源标记 文件已删，标记有意保留；不是仍存在的文档路径 注释引用 design.md 仍有多处实现文件在注释中写「承接 docs/design.md …」。那是溯源残留，不是文件仍存在。新改动勿再新增对该路径的依赖；安全与威胁模型以 README 与鉴权代码为准。 热加载措辞 对用户说「改 workdirs.json」通常没错（推荐文件名），但自动化与 env 模板必须写 WORK_DIRS_FILE 。只改文件却未设置该 env，server 不会 watch。 数字校验纪律 凡手册或 PR 描述中的计数、默认毫秒、缓冲上限，应用 rg 对照 src/ 与 scripts/agent-event-contract.js ；不要从记忆或旧 markdown 抄。本手册探针基线：dev @ 2de707b。 附注 源码探针分支为 dev（HEAD 2de707b ）；产物维护在 gh-pages 的 docs-site。 本站不随 dev 自动重生成 。发现新漂移时优先记入本页，再改叙述，避免静默「修文档盖住代码」。 已纠偏（相对初稿） 权限档为六值（含 auto ），不是「五种」；effort 不可 in-place 改活进程，须 dispose+resume；doctor 区分 CLI 11 项与 UI 体检；入向 socket 以契约 32 个、出向 agent:event 以契约 25 个为完整列表（见 事件与端点参考 ）。 2026-07-20 手册同步 基线自 8084005 抬至 2de707b 。增量主题：消息子系统 9 项修复（镜像误重锁 / pendingTurns 双扣 / 视图守卫 / pendingSendBusy / dedup TOCTOU / control_request 超时扩面 / catchUp 提交顺序）、多子代理防误杀、限流停止卡死、首页 worktree 最近活跃。契约事件数未变（入 32 / 出 25）。未 push 的 dev 段以本机 HEAD 为准。 本手册自身边界 不把 gh-pages landing 宣传文案当产品架构事实。 不把 promo 文案仓当源码。 inventory 数字（约 277 分类文件等）以生成 map 为准，会随 dev 变更。 关键数字/契约以代码与 scripts/agent-event-contract.js 为准；手册滞后时以代码胜。"}];